import { Button } from "flowbite-react";

const AddItem = () => {
  return (
    <>
      <Button className="bg-green-500 hover:enabled:bg-green-700">Copy</Button>
    </>
  );
};

export default AddItem;
